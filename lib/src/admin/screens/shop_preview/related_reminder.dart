import 'package:flutter/material.dart';

class SRelatedReminders extends StatelessWidget {
  const SRelatedReminders({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
