import 'package:flutter/material.dart';

class HeaderUserList extends StatelessWidget {
  const HeaderUserList({super.key});

  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      appBar: AppBar(
        title: Text('Header User List'),
      ),
      body: Center(
        child: Text('Header User List'),
      ),
    );
  }
}
